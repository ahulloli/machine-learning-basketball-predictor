# Copy to .env and adjust for your local PostgreSQL.
# Brew-installed postgresql@15 typically creates a superuser role matching your macOS username, no password.
DATABASE_URL=postgresql+psycopg2://rishahulloli@localhost:5432/courtvision

# Default season to ingest (NBA season string format: "2023-24")
NBA_SEASON=2023-24
